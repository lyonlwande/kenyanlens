import React from 'react';

const AddBlockPopover = ({
  show, onClose, onSelect, blockTypes
}) => {
  // ...UI and logic for block type selection popover
  return null; // Replace with actual UI
};

export default AddBlockPopover;
